class Constants {
    // Define mathematical constants
    static PI = Math.PI;
    static E = Math.E;
    static GOLDEN_RATIO = (1 + Math.sqrt(5)) / 2;
    static SQUARE_ROOT_2 = Math.sqrt(2);
    static SQUARE_ROOT_3 = Math.sqrt(3);
    // Add more constants as needed
}

module.exports = Constants;
